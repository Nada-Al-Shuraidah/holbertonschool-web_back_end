#!/usr/bin/env python3
"""Module that contains a function to return the floor of a float."""

import math


def floor(n: float) -> int:
    """Returns the floor of the float n as an integer."""
    return math.floor(n)
